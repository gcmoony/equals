import 'package:flutter/material.dart';

class LengthsPage extends StatelessWidget {
  const LengthsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Lengths");
  }
}
